{
  "name": "chilly-phone-push",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for Chilly Phone Web Push notifications",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:set": "wrangler secret put"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "jose": "^6.2.3",
    "web-push": "^3.6.7"
  }
}
