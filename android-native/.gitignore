# Build 产物
/build/
/app/build/
/.gradle/
/local.properties
*.iml
.idea/

# 签名密钥 (绝不能 commit)
android.keystore
keystore-info/
*.keystore
*.jks

# Keystore base64 临时文件
keystore.b64.txt

# PWA 打包进 APK 的资源 (build 时由 build-apk.yml 从 dist/ 复制, 不进 git, 否则 54MB)
/app/src/main/assets/
